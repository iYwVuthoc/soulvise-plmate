# 安全与隐私设计

## 报告安全问题

请不要通过公开 Issue 披露可利用漏洞、真实密钥、认证令牌或隐私截图。仓库公开后，请使用 GitHub 仓库的 **Security → Report a vulnerability** 私密报告入口，并仅提供最小化、可复现的合成材料。

维护者会先确认影响范围和复现条件，再协调修复与披露时间。本项目尚不承诺固定响应时限，也不接受通过聊天、提交信息或公开日志发送凭据。

## 威胁模型

重点防护对象包括：截图泄露、API 密钥落盘、提示注入、命令注入、恶意外部地址、请求积压、暂停后的迟到动作和敏感日志。

## 已实施控制

- 首次启动观察关闭；程序不支持隐藏监控或远程监控。
- 默认分析路径不写原始截图。视频时序判断最多在内存中保留一帧稳定期参考图且不超过15秒，并与当前帧合成分析图；停止或切换作用域后立即释放。用户在Agent中心授权Codex视觉后，只在 `%LOCALAPPDATA%\Soulvise\SoulvisePlmate\temp\vision` 独占创建随机命名临时图片；成功、失败、取消、戴眼罩、暂停或退出后立即删除，单次最长45秒。程序通过单实例锁防止互相误删，并在取得锁后立即清理上次崩溃遗留的全部合法临时图片。
- API 密钥使用系统密钥库；配置、数据库、日志、上下文和导出均不包含密钥。
- 云端提示明确把截图和窗口标题视为不可信数据，禁止执行其中指令或生成工具调用。
- 无密钥、断网或全部云端判断失败时只保留本地明确关键词结果，不以“不确定”触发强制拦截；审核与视觉分析独立降级，已成功取得的官方审核结果不会因另一路失败而丢失。
- 不使用审核接口范围过宽的总 `flagged` 值直接跳转；只有配置允许的官方类别才可强制拦截，其余类别仅保存脱敏文字事件。
- 视觉模型只能返回本次提供且已启用的规则ID；监督语义低于0.85、陪看语义低于0.80或包含未知ID时无效。Codex通用风险知识达到0.90才可拦截，政治判断仍必须依赖用户规则。
- 外部网页只允许 HTTPS 或明确的 localhost；拒绝 `file:`、`javascript:` 和公网 HTTP。拦截地址仅保存在配置中，不写入日志、观察历史或导出。
- 子进程使用可执行文件路径和参数数组，不调用 Shell，也不拼接命令字符串。
- 停止观察会使全部在途结果失效；队列最多保留最新一帧。
- 日志调用不得包含原始截图、API 密钥或完整窗口标题。
- 导出 ZIP 明确排除密钥、聊天正文、完整标题和图像。
- Codex 登录令牌由官方本机运行时托管；Soulvise 只保存非敏感会话ID，不复制令牌。
- Codex 普通聊天固定为只读沙箱并拒绝审批；临时开发会话默认只读，写入、命令和提权逐次询问，超时拒绝且不保存永久放行。
- Codex观察和认知使用与聊天、开发相互隔离的临时只读会话；不加载源码目录、拒绝全部审批和联网。流中出现命令、文件修改、网页搜索或工具事件时立即中断并废弃结果。
- Codex观察后台超时或离线时，只重建隔离观察App Server，并使用短暂退避避免重连风暴；不影响聊天会话、不扩大权限，也不静默切换到未授权服务。
- Codex返回的审核类别以Agent语义判断标记，不伪装成官方Moderation；只有用户明确开启“官方图片审核增强”后才并行调用已配置的审核API。
- 开发审批中的工作目录或文件路径一旦越出已验证的源码根目录，将在弹窗前直接拒绝。
- 本机Agent连接器只能由安装包内固定注册的代码提供；用户数据目录中的未知插件不会被动态执行。
- ACP只提供协议兼容，不等同于操作系统级安全沙箱。自定义ACP必须由用户明确选择可信程序；只有握手声明、图片授权和结构化结果自检全部通过后，才能公开视觉与语义监督能力。
- 主Agent与单项能力覆盖按配置显式路由。Provider离线、限流或缺少能力时，不静默切换到未授权厂商；本地明确规则继续工作，不确定内容不强制拦截。
- ACP观察、聊天和开发任务在逻辑上隔离；只读任务发现工具调用或权限请求会拒绝并废弃结果，开发写入仍需逐次批准。协议无法代替操作系统沙箱，因此只应连接可信Agent。
- 角色气泡的Agent润色只发送事件类型、角色状态、高兴值和本次增减量，不发送截图、窗口标题、规则文字或观察摘要；返回文本限制为24个字符并拒绝链接、Markdown、命令和多行内容。
- 桌面角色与独立气泡同时使用Windows显示捕获排除和应用内截图遮罩；平台接口失效时仍由内存遮罩兜底。
- 置顶庆祝记录只保存脱敏文字和高兴值，不包含原始截图；自动30天清理会保留它，但用户仍可单条删除或明确清空全部历史。

## 使用者责任

- 仅在本人知情设备和账户上使用，不用于秘密监控他人。
- 正式发布前完成依赖许可证清单、代码签名、渗透测试和隐私告知。
- 自定义政治规则必须由用户自行定义、审阅和维护；产品不内置不可解释的通用政治黑名单。
- 连接的Agent或API服务会接收经触发筛选后的截图；启用Codex临时图片授权或云端识别前，应理解相应服务的数据政策和费用。
- 自定义ACP程序由使用者安装和认证；连接前应核对来源、版本、权限和厂商隐私政策。

## 发布状态

- v0.2.x 是首次公开系列；仍未购买 Windows 代码签名证书，安装器可能显示“未知发布者”。
- 公开仓库、CI 与依赖更新不能证明软件不存在漏洞。正式分发前仍需进行干净虚拟机、权限边界、依赖许可和人工安全复核。
- 只有仍受维护的最新 v0.2.x 版本接受安全修复；旧个人原型仅用于本机回退。
