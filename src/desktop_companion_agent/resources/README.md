# 正式视觉素材接口

- 程序图标：`app_icon.ico`（优先）或 `app_icon.png`
- 加载页：`splash.png`，建议 620×350 或相同比例
- 角色状态：放入 `characters/`，命名规则见其 README

素材缺失不会阻止程序启动，会自动使用内置占位图。

仓库当前跟踪的 `app_icon.ico`、`splash.png` 与角色素材由项目贡献者授权按根目录 `LICENSE` 的 MIT 条款发布。替换素材时，贡献者必须确认拥有相同再许可权，并清除EXIF、作者账号和其他个人元数据。
