# 角色素材占位接口

可将正式素材按以下名称放入本目录，支持 GIF、PNG 或 WebP，优先使用 GIF：

- `normal`
- `happy`
- `angry`
- `blindfolded`
- `patted`
- `offline`

没有对应文件时，程序会绘制内置占位角色，不影响功能验收。

仓库当前六种角色动画由项目贡献者授权按根目录 `LICENSE` 的 MIT 条款发布。第三方替换素材必须使用自己拥有再许可权的文件，并保持透明背景、相同画布和脚底基准线。
